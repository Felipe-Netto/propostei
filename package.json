{
  "name": "propostei",
  "private": true,
  "scripts": {
    "dev": "concurrently --names api,web --prefix-colors cyan,magenta \"npm run start:dev --prefix api\" \"npm run dev --prefix web\""
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
